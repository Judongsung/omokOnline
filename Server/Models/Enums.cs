namespace Server.Models
{
    public enum StoneColor
    {
        None = 0,
        Black = 1,
        White = 2
    }

    public enum RoomStatus
    {
        Waiting,
        Playing,
        Finished
    }
}
